const SCREEN_SIZE = {
    width: 800,
    height: 400,
};
const BG_COLOR = [0, 0, 0];

function setUp() {
    createCanvas(SCREEN_SIZE.width, SCREEN_SIZE.height);

}

function draw() {
    background(BG_COLOR);
}
